# Gemini Developer API key (https://aistudio.google.com/app/apikey).
# Format starts with "AQ." — used by both the brief and image endpoints.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: pin a different image model (default is gemini-2.5-flash-image).
# MYTHIC_IMAGE_MODEL=gemini-2.5-flash-image

# Optional: environmental atmosphere weaved into the narrative.
# MYTHIC_ENV_CURRENT=The lush, salt-mist jungle of Nosara, Costa Rica
# MYTHIC_ENV_ANCHOR=The limestone, dry-creek bedrock of Austin, Texas
